package gearbox

import "github.com/sunyakun/gearbox/pkg/apis"

type Object = apis.Object

type ObjectMeta = apis.ObjectMeta

type Status = apis.Status

type ListOptions = apis.ListOptions

type RawExtension = apis.RawExtension

var NewScheme = apis.NewScheme
